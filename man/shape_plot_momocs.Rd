% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_plot_momocs.R
\name{shape_plot_momocs}
\alias{shape_plot_momocs}
\title{Plot PCA results with shapes at convex hull points.}
\usage{
shape_plot_momocs(
  pca_result,
  coo_object,
  pc_x = 1,
  pc_y = 2,
  shape_size = 0.01,
  shift_distance = 0,
  vertical_offset = 0,
  horizontal_offset = 0,
  title = NULL,
  x_label = NULL,
  y_label = NULL,
  point_color = "black",
  point_size = 2,
  title_size = 24,
  label_size = 20,
  tick_size = 15,
  tick_length = 0.005,
  arrow_shift = 0,
  x_label_adjust = 0,
  y_label_adjust = 0
)
}
\arguments{
\item{pca_result}{The PCA result object from \code{Momocs::PCA()}.}

\item{coo_object}{The Coo object from which shapes are extracted.}

\item{pc_x}{(Optional) The principal component for the x-axis. Default is 1.}

\item{pc_y}{(Optional) The principal component for the y-axis. Default is 2.}

\item{shape_size}{(Optional) The scaling factor for the shapes. Default is 0.01.}

\item{shift_distance}{(Optional) The distance to shift the shapes from the plot center. Default is 0.}

\item{vertical_offset}{(Optional) The vertical offset applied to the shapes. Default is 0.}

\item{horizontal_offset}{(Optional) The horizontal offset applied to the shapes. Default is 0.}

\item{title}{(Optional) The title of the plot. Default is NULL (no title).}

\item{x_label}{(Optional) Label for the x-axis. Default is NULL (uses PC axis label).}

\item{y_label}{(Optional) Label for the y-axis. Default is NULL (uses PC axis label).}

\item{point_color}{(Optional) Color of the PCA scatter points. Default is "black".}

\item{point_size}{(Optional) Size of the PCA scatter points. Default is 2.}

\item{title_size}{(Optional) Font size for the plot title. Default is 24.}

\item{label_size}{(Optional) Font size for the axis labels. Default is 20.}

\item{tick_size}{(Optional) Font size for the tick labels. Default is 15.}

\item{tick_length}{(Optional) Length of the tick marks, relative to plot size. Default is 0.005.}

\item{arrow_shift}{(Optional) Amount to shift the arrowed axes to avoid overlap with tick marks. Default is 0.}

\item{x_label_adjust}{(Optional) Adjustment for the x-axis label position. Default is 0.}

\item{y_label_adjust}{(Optional) Adjustment for the y-axis label position. Default is 0.}
}
\value{
A scatterplot of PCA results with shapes displayed at the convex hull points.
}
\description{
This function generates a scatterplot from PCA results with shapes displayed at the convex hull points.
The shapes can be adjusted for size, position, and orientation, and the plot includes custom axis labels,
tick marks, and title adjustments. Aspect ratio is maintained to prevent shape distortion.
}
\examples{
# Load the Momocs package
library(Momocs)

# Example using the 'bot' dataset (botanical leaves) from Momocs
bot_shapes <- bot \%>\% coo_center() \%>\% coo_scale() \%>\% coo_align()

# Perform a Fourier transformation
bot_fourier <- bot_shapes \%>\% efourier(nb.h = 10)

# Perform PCA on the Fourier coefficients
bot_pca <- PCA(bot_fourier)

# Plot PCA results with shapes at convex hull points
shape_plot_momocs(bot_pca, bot_shapes,
                  pc_x = 1, pc_y = 2,  # PC axes for plotting
                  shape_size = 0.01, shift_distance = 0.1,
                  vertical_offset = 0, horizontal_offset = 0,
                  title = "PCA Shape Plot", x_label = "PC1", y_label = "PC2")
}
